# The Command Line, working in the terminal on Windows or Linux/Mac

<a title="Jason Scott, CC BY 2.0 &lt;https://creativecommons.org/licenses/by/2.0&gt;, via Wikimedia Commons" href="https://commons.wikimedia.org/wiki/File:DEC_VT100_terminal.jpg"><img width="330" alt="DEC VT100 terminal" src="https://thumb.wikimedia.org/wikipedia/commons/thumb/9/99/DEC_VT100_terminal.jpg/330px-DEC_VT100_terminal.jpg?utm_source=commons.wikimedia.org&utm_campaign=index&utm_content=thumbnail"></a>

There was a time that computers only could show text on the screen, and the user only could enter text on a keyboard. No graphics, no mouse. That's long time ago. Though graphical user interaction is widespread,  the textual interaction with computers has never gone. Especially system administrators as well as developers always have been using textual interaction, besides graphical interaction. Why is that? Giving textual commands has several advantages:
- single commands can do very powerful tasks
- sequence of commands can be stored in a file, called a script, and be evaluated
- commands can be documented precisely (no need to describe what is graphically visible and mouse movements)
- as all text, this can be generated by LLM's (can be dangerous!)

Sometimes the terminal is part of an editor, such as in VS-code, so you do not need to open a dedicated terminal to enter commands (though you can do so). The following situations are examples, when working in the terminal is very effective:
- installing python packages with [`pip`](https://pip.pypa.io/en/stable/getting-started/) (or [`uv`](https://docs.astral.sh/uv/))
- evaluating git commands
- installing software under Linux, e.g. in Ubuntu you can do this with `apt install ...` (under Windows, you can do similarly with [`winget`](https://learn.microsoft.com/en-us/windows/package-manager/winget/))
- working with ROS under Ubuntu Linux


## Windows
The most popular terminals in windows are:
- Powershell (modern) (e.g. [geek for geeks windows powershell tutorial](https://www.geeksforgeeks.org/techtips/windows-powershell-tutorial/))
- Cmd (classic) (e.g. [geek for geeks what is command prompt](https://www.geeksforgeeks.org/operating-systems/what-is-a-command-prompt/) and [command prompt tutorial](https://www.geeksforgeeks.org/techtips/windows-command-prompt-tutorial/))


## Linux/Mac
On Ubuntu one usually can open the default terminal quickly by pressing `<Ctrl><Alt>T`, most likely the `gnome-terminal` is opened (other terminals are `Terminator`, `Alacritty` and `Urxvt`). A good guide is [The Linux command line for beginners](https://ubuntu.com/tutorials/command-line-for-beginners#1-overview).
